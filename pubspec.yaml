name: lean_provider
description: A simple provider boilerplate.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  syncfusion_flutter_gauges: ^1.0.0-beta
  flutter_sparkline: ^0.1.0

  # Provider
  get_it: ^3.0.3
  provider: ^3.1.0+1
  connectivity: ^0.4.6+1
  rxdart: ^0.23.1
  dio: ^3.0.7
  flutter_slidable: ^0.5.3
  intl: ^0.16.0
  auto_size_text: ^2.0.1

  hive: ^1.3.0
  hive_flutter: ^0.3.0+1

  cupertino_icons: ^0.1.2

dev_dependencies:
  hive_generator: ^0.7.0
  build_runner: ^1.7.3

  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
